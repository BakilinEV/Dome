
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	//Таблица = ЭтотОбъект.Выгрузить();
	//Таблица.ЗаполнитьЗначения(Пользователи.ТекущийПользователь(), "Ответственный");
	//Таблица.ЗаполнитьЗначения(ИмяКомпьютера(), "ИмяКомпьютера");
	//
	//ЭтотОбъект.Загрузить(Таблица);
	
КонецПроцедуры
